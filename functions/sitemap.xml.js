export async function onRequestGet(context){const origin=new URL(context.request.url).origin;const result=await context.env.DB.prepare("SELECT slug,updated_at FROM articles WHERE status='published' AND published_at<=CURRENT_TIMESTAMP ORDER BY published_at DESC LIMIT 5000").all();const staticPaths=['','about','editorial-policy','corrections'];const urls=[...staticPaths.map((path)=>({loc:`${origin}/${path}`,lastmod:null})),...result.results.map((item)=>({loc:`${origin}/article/${encodeURIComponent(item.slug)}`,lastmod:item.updated_at}))];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map((item)=>`<url><loc>${item.loc}</loc>${item.lastmod?`<lastmod>${item.lastmod.slice(0,10)}</lastmod>`:''}</url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml; charset=utf-8','Cache-Control':'public, max-age=600'}});}
