# 시니어 라이프 뉴스 작업 인수인계 — 2026-09-01

## 서비스 및 배포 환경

- 서비스명: 시니어 라이프 뉴스
- 운영 주소: `https://seniorlifenews.pages.dev`
- Cloudflare Pages 프로젝트명: `seniorlifenews`
- GitHub 저장소: `gumdalf7787art/seniornews` (`main` 브랜치)
- D1 데이터베이스: `senior-db`
- R2 버킷: `senior-r2`
- R2 공개 개발 URL: `https://pub-d47865c644f54ee7b9101675b52db9bd.r2.dev`
- Cloudflare Pages Functions 바인딩:
  - D1: `DB`
  - R2: `BUCKET`
  - 공개 미디어 URL 환경 변수: `PUBLIC_MEDIA_URL`
  - JWT 비밀값: `JWT_SECRET` (Cloudflare Secret으로 설정, 값은 문서에 기록하지 않음)

`seniornews.pages.dev`는 사용하지 않는다. 운영 대상은 반드시 `seniorlifenews.pages.dev` 하나다.

## 현재 구현 완료 항목

### 공개 뉴스 화면

- 홈, 카테고리, 검색, 기사 상세 화면이 기존 로컬 더미 배열이 아니라 D1의 `published` 기사만 읽도록 전환했다.
- 공개 API:
  - `GET /api/articles`
  - `GET /api/articles/:slug`
- 공개 기사 목록은 발행 시각이 현재보다 이전인 기사만 노출한다.
- 홈의 주요 뉴스·최신 뉴스·많이 본 뉴스, 카테고리의 기사 목록, 기사 상세의 관련 기사와 우측 인기·최신 뉴스가 실제 D1 기사 기준으로 표시된다.
- 기존 더미 기사는 UI 표시용으로 사용하지 않는다. `src/data/articles.js`에는 카테고리·라벨 등 일부 정적 정의가 남아 있다.

### 역할과 기사 발행 흐름

- 역할: `reader`(일반회원), `editor`(관리자/기자), `admin`(제작자).
- `goodduck2@naver.com`은 제작자(`admin`)로 변경되어 있다.
- 기사 상태 UI:
  - `draft` → 작성 중
  - `review` → 발행 요청 (기존 DB 상태값은 호환성을 위해 유지)
  - `published` → 공개 중
- 기자는 본인이 작성한 기사를 수정할 수 있고, 발행 요청 또는 직접 발행할 수 있다.
- 제작자는 전체 기사를 수정하고 발행할 수 있다.
- 공개 중인 기사를 기자가 수정하면 발행 요청 상태로 다시 보낼 수 있다.
- 서버에서 작성자/역할을 검사하며, 기자는 다른 사람의 기사를 수정하거나 발행할 수 없다.

### 기사 편집기

- 블록 기반 본문 편집기를 구현했다.
  - 문단, 소제목, 인용문, 본문 이미지 블록
  - 블록 이동 및 삭제
  - 본문 이미지 설명(대체 텍스트)과 캡션
- 대표 이미지와 본문 이미지에는 이미지 설명이 필요하며, 저장/발행 전 검증한다.
- 기사 상세에서 블록 본문과 삽입 이미지를 렌더링한다.

### 대표 이미지 업로드 — 최신 구조

- 대표 이미지 파일을 선택하면 먼저 브라우저의 로컬 썸네일을 표시한다.
- 파일을 고르는 순간에는 R2에 저장하지 않고, `임시 저장`·`발행 요청`·`직접 발행` 시 R2 업로드 후 실제 URL을 기사에 저장한다.
- 이미지 URL 입력값과 로컬 썸네일 상태를 분리했다. 따라서 URL 입력 필드 때문에 `blob:` 미리보기가 사라지지 않도록 했다.
- 이미지 상태 영역에 다음을 표시한다.
  - 파일 준비 중
  - 첨부 완료 · 저장 대기
  - R2 업로드 중
  - R2 업로드 완료
  - 업로드 실패/파일 처리 실패
- JPG·PNG·WEBP 외의 일반 이미지나 5MB 초과 이미지는 브라우저에서 2,200px 이내의 JPEG로 자동 변환해 업로드를 시도한다.
- 파일이 이미지가 아니거나 자동 변환에 실패하면 화면 안내와 브라우저 모달(`alert`)을 함께 보여 준다.
- R2 저장 API는 `File.arrayBuffer()`로 파일 데이터를 R2에 저장한다.
- R2 자체 점검 완료:
  - 임시 진단 파일을 `senior-r2`에 업로드
  - 공개 URL에서 HTTP 200 응답 확인
  - 진단 파일 삭제 완료
  - 따라서 R2 버킷과 공개 URL 자체는 정상이다.

## 중요: 이미지 기능 테스트 절차

1. `/admin`에서 기존 기사 `수정` 또는 새 기사 작성으로 들어간다.
2. 대표 이미지 설명을 입력한다.
3. `이미지 업로드`를 눌러 파일을 선택한다.
4. 대표 이미지 영역에서 썸네일과 `첨부 완료 · 저장 대기`가 보이는지 확인한다.
5. `발행 요청` 또는 `직접 발행`을 누른다.
6. 상태가 `R2 업로드 중` → `R2 업로드 완료`로 바뀐 뒤 목록으로 돌아가는지 확인한다.
7. 홈 또는 해당 카테고리/기사 상세에서 이미지가 표시되는지 확인한다.

이미지 저장 실패 시에는 이미지 영역의 상태와 화면 상단 안내 문구를 캡처해서 확인한다. 현재까지 원격 D1의 `media` 테이블에는 과거 실패 시도에 대한 저장 기록이 없었다.

## 인증

- 이메일 회원가입·로그인 구현 완료.
- 로그인 성공 후에는 홈으로 이동한다.
- 카카오 로그인 구현 및 동작 확인.
  - 로그아웃 뒤 재로그인 시 카카오 재인증을 요구하도록 `prompt=login`을 사용한다.
  - 카카오톡 인앱 환경에서는 카카오 플랫폼의 세션 정책에 따라 동작 차이가 날 수 있다.
- 네이버 로그인 구현 및 설정 완료.
- OAuth 키·클라이언트 시크릿·JWT 비밀값은 Git 또는 문서에 기록하지 않는다. Cloudflare Pages의 Variables and secrets에서만 관리한다.

## 데이터/보안 관련

- 비밀번호: 안전한 salt 기반 scrypt 해시 사용.
- JWT 쿠키 세션 사용.
- 관리자 API에서 역할을 서버 측으로 검증.
- 변경 요청은 `X-Requested-With: SeniorNews`와 동일 출처 검사를 사용.
- 미디어 업로드는 이미지 형식과 저장소 설정을 확인한다.

## 최근 주요 커밋

| 커밋 | 내용 |
| --- | --- |
| `778cba9` | 이미지 파일 처리 실패 시 모달 안내 |
| `e34a4fa` | 대표 이미지 상태 UI, 자동 변환, 지속 썸네일 |
| `c46f2ee` | 이미지 미리보기와 기사 저장 흐름 유지 |
| `e358a72` | 이미지 미리보기 및 공개 기사 수정 권한 수정 |
| `86763fa` | 공개 페이지에 D1 발행 기사 표시 |
| `82575a5` | 발행 요청·기자 직접 발행 구현 |
| `13a9195` | 블록 편집기와 본문 이미지 삽입 |

최신 커밋은 `778cba9`이며 Cloudflare Pages Production 활성 배포를 확인했다.

## 검증 결과

- `npm.cmd run build` 통과
- `npm.cmd run lint` 통과 (React Fast Refresh 및 effect 관련 경고만 존재)
- `npm.cmd test` 통과
- `npx.cmd wrangler@3.114.17 pages functions build --outdir .wrangler/functions` 통과

`node:crypto` 관련 Wrangler 경고가 출력되지만 `wrangler.toml`에 `nodejs_compat` 플래그가 설정돼 있으며, Pages 배포는 정상 활성 상태다.

## 다음 작업 권장 순서

1. 최신 배포에서 7MB 이미지로 대표 이미지 업로드 전체 흐름을 실제 계정으로 재시험한다.
2. 이미지가 저장된 뒤 D1의 `media` 테이블과 `articles.image_url` 값이 생기는지 확인한다.
3. 기사 작성 UI를 더 단순하게 개편한다. 대표 이미지는 드래그앤드롭, 명확한 파일 카드, 교체/삭제 버튼을 추가하는 방향이 적합하다.
4. 공개 페이지의 기사 수가 적을 때 홈 레이아웃이 과하게 비지 않도록 빈 상태/단일 기사 레이아웃을 다듬는다.
5. 운영용 콘텐츠 작성, 이미지 저작권·출처 관리, SEO 메타데이터의 실제 데이터 연동을 계속 진행한다.

