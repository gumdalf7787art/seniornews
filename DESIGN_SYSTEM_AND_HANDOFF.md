# 시니어 라이프 뉴스 · 시니어 라이프 연구소 디자인 및 작업 인수인계

최종 갱신: 2026-09-01  
운영 주소: `https://seniorlifenews.pages.dev`  
저장소: `gumdalf7787art/seniornews` (`main`)

> 운영 대상은 `seniorlifenews.pages.dev` 하나다. `seniornews.pages.dev`는 사용하지 않는다.
> API 키, OAuth 시크릿, JWT 값 등 비밀값은 이 문서나 Git에 기록하지 않고 Cloudflare Pages의 **Variables and secrets**에서만 관리한다.

---

## 1. 서비스 구조

`(주)메디프라퍼`는 두 개의 서로 연결된 서비스를 운영한다.

| 서비스 | 역할 | 기본 경로 |
| --- | --- | --- |
| 시니어 라이프 뉴스 | 시니어가 오늘 바로 활용할 뉴스와 생활 정보를 전하는 편집형 뉴스 채널 | `/` |
| 시니어 라이프 연구소 | 시니어의 건강·자립·관계·경제·디지털 환경을 연구하고 인사이트와 리포트를 축적하는 조직 | `/lab` |

- 두 서비스는 하나의 도메인 안에 있으나, 방문자는 서로 다른 서비스로 분명하게 인식해야 한다.
- 뉴스는 **정보 전달과 일상성**, 연구소는 **근거·탐구·신뢰**를 우선한다.
- 두 서비스는 동일한 블루·인디고·바이올렛 컬러 체계, Pretendard 기반 서체, 넓은 여백과 명확한 대비를 공유한다.

---

## 2. 공식 브랜드 컬러

### 핵심 토큰

`src/index.css`의 `:root`를 단일 기준으로 사용한다.

| 이름 | 값 | 사용 원칙 |
| --- | --- | --- |
| Brand Navy | `#0b153e` | 깊은 신뢰감이 필요한 헤더 배경, 구분선, 핵심 텍스트 |
| Brand Indigo | `#5457c8` | 활성 메뉴, 링크, 보조 강조, 버튼의 중심 색 |
| Life Blue | `#73a8ff` | 밝은 시작점, 포커스 아웃라인, 그라데이션의 왼쪽 |
| Ink Violet | `#7b5cc7` | 그라데이션의 마무리, 연구소·브랜드의 확장감 |
| Surface Indigo | `#eef0ff` | 연한 강조 영역, 칩, 보조 배경 |
| Surface Soft | `#f1f3fa` | 보조 카드·배경 |
| Paper | `#f7f8fc` | 전체 기본 배경 |
| Ink | `#18233e` | 본문과 제목의 기본 텍스트 |
| Ink Subtle | `#4f5c76` | 요약·설명 텍스트 |
| Line | `#dce3f0` | 경계선·구분선 |
| Muted | `#65708a` | 메타 정보·비활성 텍스트 |

### 포인트 색상 정책

- 과거 주황색 포인트는 더 이상 브랜드 핵심색으로 사용하지 않는다.
- 기존 컴포넌트 호환을 위해 `--orange`는 `var(--brand-indigo)`에 연결되어 있다.
- 링크·선택 상태·순위 등의 포인트는 기본적으로 **Brand Indigo**를 쓴다.
- 강한 브랜드 장면(메인 타이틀, 연구소 버튼, 히어로 배경)에서만 **Life Blue → Indigo → Ink Violet** 그라데이션을 쓴다.
- 버튼과 배경이 같은 톤으로 겹쳐 가독성이 떨어지지 않게, 기본 텍스트에는 `Ink`/흰색을 사용한다.

### 공식 그라데이션

| 용도 | CSS 예시 |
| --- | --- |
| 어두운 뉴스 메인 타이틀 배경 | `linear-gradient(112deg, #071331 0%, #0d1640 48%, #25104b 100%)` |
| 뉴스 워드마크 글자 | `linear-gradient(100deg, #8ec2ff 0%, #a0a3ff 47%, #d1a6ff 100%)` |
| 연구소 흰 헤더 워드마크 | `linear-gradient(100deg, #6c9fff 0%, #6479e4 50%, #32136e 100%)` |
| 연구소의 뉴스 바로가기 버튼 | `linear-gradient(112deg, #385da8, #4d50af 53%, #7446a4)` |

---

## 3. 공식 워드마크(로고) 원칙

### 정의

이 서비스에서 공식 로고는 기존의 원형 `시` 아이콘이 아니라, 다음 세 요소가 결합된 **타이포그래피 워드마크**다.

1. 한글 서비스명
2. 영문 부제
3. 영문 아래의 얇은 블루→바이올렛 그라데이션 라인

따라서 로고를 새로 적용할 때 원형 아이콘을 임의로 앞에 붙이지 않는다. 워드마크는 HTML/CSS로 구현되어 어느 화면에서도 선명하다.

### 뉴스 워드마크

- 한글: `시니어 라이프 뉴스`
- 영문: `SENIOR LIFE NEWS`
- 한글: 900 굵기, 타이트한 자간(`letter-spacing: -.09em`), 블루→라일락 그라데이션
- 영문: Arial 계열, 넓은 자간(`.34em`), 연한 푸른 흰색
- 라인: 좌우는 투명하게 사라지고 가운데에서 밝아지는 그라데이션

### 연구소 워드마크

- 한글: `시니어 라이프 연구소`
- 영문: `SENIOR LIFE LAB`
- 한글: 900 굵기, 블루→인디고→딥 바이올렛 그라데이션
- 영문: 네이비 계열의 넓은 자간
- 라인: 밝은 블루→라벤더→바이올렛
- 연구소는 흰 배경에서도 읽히는 형태를 기본으로 한다.

---

## 4. 뉴스 헤더 구조

구현 위치: `src/components/NewsLayout.jsx`, `src/index.css`

### 데스크톱 메인 헤더

1. **어두운 브랜드 마스트헤드**
   - 어두운 네이비→바이올렛 그라데이션 배경
   - 중앙: 뉴스 공식 워드마크
   - 왼쪽: `오늘을 더 잘 살고 / 내일을 든든하게 준비하는 뉴스`
   - 오른쪽: `시니어 라이프 연구소 / Senior Life Lab` 버튼 (`/lab`)
   - 상단 우측 소형 영역: 검색, 보기 설정, 로그인/회원가입
   - 좌우의 얇은 곡선 라인은 브랜드의 흐름·연결성을 표현하는 장식이다.

2. **뉴스 카테고리 바**
   - 메뉴: 주요뉴스 · 건강 · 복지·정책 · 생활·금융 · 일자리 · 디지털 · 문화·여가
   - 평상시: 밝은 배경, Brand Indigo 활성 텍스트
   - 스크롤 후 상단 고정: 네이비→바이올렛 배경으로 반전
   - 고정 상태에서는 왼쪽에 축소 뉴스 워드마크, 오른쪽에 연구소 버튼을 표시한다.
   - 현재 카테고리는 인디고 배경/밝은 텍스트로 인식 가능해야 한다.

### 뉴스 헤더의 전환 규칙

- 상단 고정 카테고리 바는 스크롤 위치에 따라 한 번만 자연스럽게 전환되어야 한다.
- 고정된 상태에서 다른 뉴스 카테고리를 누르면 다음 페이지도 **고정 카테고리 바 상태**로 시작해야 한다.
- 상단 마스트헤드가 보이는 상태에서 메뉴를 누르면 일반 뉴스 헤더 전체가 보이는 상태로 이동한다.
- 모바일에서는 데스크톱 검색/로그인 영역을 숨기고, 하단 고정 메뉴 및 시트로 제공한다.

---

## 5. 시니어 라이프 연구소 전용 헤더

구현 위치: `src/pages/LabPage.jsx`, `src/index.css`

### 경로와 전용 레이아웃

연구소 경로는 뉴스 공통 `NewsLayout` 밖에 배치되어 있다. 즉 `/lab`에서는 뉴스의 마스트헤드·뉴스 카테고리·뉴스 푸터가 보이지 않는다.

| 경로 | 화면 |
| --- | --- |
| `/lab` | 연구소 홈 |
| `/lab/about` | 연구소 소개 |
| `/lab/research` | 연구 아카이브 |
| `/lab/research/:slug` | 연구·리포트 상세 |
| `/lab/insight` | 연구소 인사이트 |
| `/lab/contact` | 협력 문의 |

### 데스크톱 헤더 구성

- **배경:** 흰색. 뉴스와 다르지만 같은 컬러 체계로 연결되는 인상을 준다.
- **왼쪽:** `건강한 삶의 후반전을 / 연구합니다.`
  - Life Blue→Indigo→Violet 그라데이션 텍스트
  - 연구소의 목적을 간결히 설명하는 문구
- **가운데:** 연구소 공식 워드마크
  - `시니어 라이프 연구소`
  - `SENIOR LIFE LAB`
  - 하단 그라데이션 라인
- **오른쪽:** 브랜드 배경 버튼
  - `시니어 라이프 뉴스`
  - `Senior Life News`
  - 클릭 시 `/`로 이동
  - 흰색이나 회색 테두리 버튼이 아니라 블루→인디고→바이올렛 그라데이션 버튼을 사용한다.
- **아래 메뉴:** 연구소 홈 · 연구소 소개 · 연구 아카이브 · 인사이트 · 협력 문의
  - 활성 메뉴는 Brand Indigo 텍스트와 하단 인디고 라인

### 모바일 연구소 헤더

- 좁은 화면에서는 왼쪽 문구를 숨겨 워드마크와 뉴스 바로가기 버튼의 가독성을 우선한다.
- 연구소 메뉴는 가로 스크롤이 가능한 한 줄 메뉴로 유지한다.
- 헤더의 흰색 배경과 워드마크·버튼의 컬러 대비를 유지한다.

### 연구소 하단

- 연구소 전용 하단에는 `(주)메디프라퍼` 표기와 `시니어 라이프 뉴스로 돌아가기` 링크를 제공한다.
- 뉴스 푸터의 상세 법인 정보와 중복되지 않도록 간결하게 유지한다.

---

## 6. 연구소 콘텐츠 기준

### 연구 주제

1. 건강한 노화와 예방
2. 자립 생활과 돌봄
3. 경제·일자리와 사회참여
4. 디지털 포용과 생활 기술

### 초기 콘텐츠 형식

- 이슈 브리프
- 연구 노트
- 정책 브리프
- 인사이트

초기 공개 페이지는 연구소의 활동 방향을 보이는 소개용 콘텐츠다. 실제 조사·통계·인용을 추가할 때에는 출처, 조사 범위, 방법, 발행일을 함께 기록한다. 다운로드 파일이 준비되기 전에는 버튼을 활성화하지 않고 `요약본 준비 중`으로 표시한다.

---

## 7. 접근성·반응형 공통 기준

- 기본 서체: `Pretendard`, 대체 `Apple SD Gothic Neo`, system UI
- 기본 본문 크기: 기사 본문 `20px` (`--article-size`)
- 큰 글자 모드: `23px`
- 주요 터치 대상: 최소 `44×44px`, 주요 폼 버튼은 `48px` 이상
- 키보드 포커스: `Life Blue` 3px 외곽선
- 고대비 모드: 검정·흰색 중심으로 토큰을 교체
- 모바일 기본 여백: 좌우 `16px`
- 고정 메뉴 및 안전 영역을 고려해 모바일 본문 하단 여백을 확보
- 애니메이션과 자동 전환은 `prefers-reduced-motion` 환경을 존중한다.

---

## 8. 운영·개발 상태 요약

### 배포와 저장소

- Cloudflare Pages 프로젝트: `seniorlifenews`
- D1: `senior-db` (`DB` 바인딩)
- R2: `senior-r2` (`BUCKET` 바인딩)
- R2 공개 URL 환경 변수: `PUBLIC_MEDIA_URL`
- GitHub 기본 브랜치: `main`

### 사용자 역할

| DB 역할 | 서비스 표기 | 권한 요약 |
| --- | --- | --- |
| `reader` | 일반회원 | 북마크·마이페이지 |
| `editor` | 기자 | 기사 작성, 발행 요청, 직접 발행 |
| `admin` | 관리자 | 전체 기사·회원·카테고리·배너·알림 운영 |

### 기사 및 미디어 운영 원칙

- 공개 화면은 D1의 발행 완료 기사만 표시한다.
- 대표 이미지는 R2에 저장하며, 편집기에서는 로컬 미리보기와 업로드 상태를 분명히 보여준다.
- JPG·PNG·WEBP 이미지는 웹용 처리 흐름을 거친다.
- 10MB 초과 이미지는 업로드 전에 명확한 안내 모달을 표시한다.
- 이미지에는 대체 텍스트/설명을 입력할 수 있어야 한다.

---

## 9. 최근 관련 커밋

| 커밋 | 내용 |
| --- | --- |
| `e49f47e` | 연구소 헤더의 왼쪽 미션 문구와 브랜드 버튼 개선 |
| `84f8dd1` | 뉴스 레이아웃에서 연구소를 분리하고 전용 헤더 적용 |
| `cc9a405` | `/lab` 연구소 홈·소개·아카이브·인사이트·문의 섹션 추가 |
| `dedfebd` | 회원정보 이름 수정 및 비밀번호 변경 기능 |

---

## 10. 다음 작업 시 주의사항

1. 뉴스와 연구소의 워드마크를 이미지로 대체하지 말고 HTML/CSS 타이포그래피로 유지한다.
2. 새 UI에서 과거의 주황색을 임의의 강조색으로 다시 도입하지 않는다. 필요한 강조는 Brand Indigo 또는 브랜드 그라데이션을 쓴다.
3. `/lab/*`에 뉴스 공통 헤더나 뉴스 카테고리 바를 다시 넣지 않는다.
4. 연구소 연구물에 실제 수치·근거를 게시할 때 출처와 발행 정보를 함께 관리한다.
5. Cloudflare의 실제 비밀값과 OAuth 정보는 절대 커밋하지 않는다.
6. `public/brand-concepts/`는 현재 미추적 디자인 참고 자산이다. 사용자가 별도 요청하기 전에는 커밋하지 않는다.

